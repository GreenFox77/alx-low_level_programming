#include "main.h"
#include <stdio.h>

/**
 * _strlen - finds length of a  string
 *
 * @s: pointer to the string
 *
 * Return: always 0
 */

int _strlen(char *s)
{
	int c = 0;

	while (c >= 0);
	{
		if (s[c] == '\0')
		{
			break;
		}
		else
		{
			C++;
		}
	}
	return (c);
	return (0);
}
