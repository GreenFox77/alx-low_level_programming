#include <stdio.h>


/**
 * main - prints the name of the program
 * @argc: counts the number of arguments
 * @argv: vector of arguments (an array of strings)
 * Return: Always 0
 */
int main (__attribute__((unsed)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
